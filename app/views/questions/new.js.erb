var $form = $('#addQuestionForm');
// var $links = $('.questionLinks');

// myApp.disableLinksConditionally($form, $links);
myApp.Questions().toggleAll();

$form.html("")
	.append('<%= j render :partial => "questions/new", :locals => { :question => @question, :quiz => @quiz, :answer => @answer } %>')
	.slideToggle();

myApp.multipleChoiceQuestion();

<%= form_for [@lecture, @question] do |f| %>
<div class="inline textArea">
	<%= f.label :content %>
	<%= f.text_area :content, :placeholder => "Enter question" %>	
</div>
<%= f.hidden_field :order_index, :value => @index %>
<div>
	<%= f.submit %>
</div>
<% end %>
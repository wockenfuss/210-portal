<%= link_to "<– Back", user_root_path(@user) %>
<h2><%= @quiz.name %></h2>

<% if @quiz.questions.count > 0 %>
	<p><%= "There are #{@quiz.questions.count} questions in this quiz." %></p>
	<p><%= "You'll have #{@quiz.duration} minutes to complete this quiz once you begin." %></p>
	<p><%= "You only get one attempt at the quiz. If you navigate away from the page while taking the quiz, it will be marked complete, and you won't be able to resume your attempt." %></p>
	<!--<#%= button_tag "Begin Quiz", new_attempt_path(@quiz, @user)%>-->
	<!--<#%= button_to('Begin Quiz', new_attempt_path(@quiz, @user), :confirm => 'Are you sure?',
	          :method => "post", :remote => true ) %>-->
	<%= button_to "Begin Quiz", new_attempt_path, :method => 'post', :class => 'button', :id => 'new_attempt_btn', :confirm => "Are you sure you're ready?" %>
<% else %>
	<p>There are no questions in this quiz</p>
<% end %>